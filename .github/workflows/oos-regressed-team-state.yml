name: Multi-season regressed team state OOS

on:
  push:
    branches:
      - feature/slate-simulator-v1
    paths:
      - scripts/validate_oos_regressed_team_state.py
      - src/monster/feature_compile/team.py
      - src/monster/snapshot/league.py
      - src/monster/sim/game.py
      - .github/workflows/oos-regressed-team-state.yml
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
      - run: uv sync
      - name: Run multi-season team-state ablation
        run: >-
          uv run python scripts/validate_oos_regressed_team_state.py
          --worlds 2500 --seed 2026090737
          --out artifacts/oos-regressed-team-state
      - name: Show result
        if: always()
        run: cat artifacts/oos-regressed-team-state/manifest.json
      - name: Enforce regression gate
        run: >-
          uv run python -c "import json; d=json.load(open('artifacts/oos-regressed-team-state/manifest.json')); assert d['robustness_pass'], d"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: monster-oos-regressed-team-state
          path: artifacts/oos-regressed-team-state/
          retention-days: 7
